# Architecture

System and subsystem architecture documents for LiteAero Flight.

| Document | Contents |
| --- | --- |
| [dynamic_element.md](dynamic_element.md) | **Design authority** for all SISO dynamic elements — `DynamicElement` NVI pattern, serialization contract, logging interface |

Additional architecture documents are added as subsystems are designed and implemented.
For the current design authority documents, see
[liteaero-sim/docs/architecture/](../../liteaero-sim/docs/architecture/) — documents
for subsystems not yet migrated to liteaero-flight remain there until their migration
step is complete.
